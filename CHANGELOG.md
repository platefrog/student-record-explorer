# 변경 이력

## v1.1.3

- 실제 나이스 항목별 조회 원본이 `.xlsx`로 내려오는 경우도 전처리에 사용할 수 있도록 수정했습니다. `.xlsx.data`와 `.xlsx`를 모두 허용합니다.

## v1.1.2

- 나이스 학생부 원본 `.xlsx.data` 파일이 실제 XLSX MIME 형식으로 판정되어 업로드 단계에서 거부되던 문제를 수정했습니다.
- 업로더는 XLSX MIME을 함께 허용하되, 전처리 직전에 파일명이 `.xlsx.data`인지 다시 확인해 일반 `.xlsx` 파일은 명확한 안내와 함께 차단합니다.

## v1.1.1

- 커리어넷 학과 정보 501개 재수집 원자료를 적용할 수 있도록 말뭉치 스키마 v3와 갱신 절차를 정리했습니다.
- 통합 말뭉치에서 대학주요교과목의 중복 결합을 제거했습니다.
- Windows 시스템 인증서 저장소를 사용하는 `truststore` 지원과 인증서 오류 안내를 추가했습니다.
- 수집 오류 메시지와 URL에서 API 키를 `[REDACTED]`로 마스킹합니다.
- 표현 통일 별칭을 보호 토큰으로 처리하고, 어휘 전달 감사 도구를 추가했습니다.
- 모든 분석기에서 기본 최소 단어 길이 2를 일관되게 적용합니다.
- SQLite 연결을 명시적으로 닫아 Windows에서 말뭉치·캐시 DB가 잠기는 문제를 막았습니다.

## v1.1.0

- 학생 TF-IDF 계산, 전체 벡터 연구지표, 앱 캐시 저장 범위와 화면 표시 범위를 분리했습니다.
- 전체 학생 문서의 희소행렬에서 전체 양의 TF-IDF 벡터 지표를 산출하고 학생별 상위 120개 특징어만 캐시에 저장합니다.
- `student_metrics`, `tfidf_metrics` 테이블과 캐시 스키마 v3 재현 메타데이터를 추가했습니다.
- 기록 구성 정보와 학과 말뭉치 어휘 포착률을 학생 분석 화면에 추가했습니다.
- TF-IDF·빈도 워드클라우드 가중치와 표시 단어 수를 사용자가 선택할 수 있게 했습니다.
- 구버전 캐시를 감지해 기존 자료의 사용 가능 범위와 재전처리 필요성을 안내합니다.
- 연구용 희소행렬 번들, 구·신 캐시 회귀 비교와 후보 안정성 검증 스크립트를 추가했습니다.

## v1.0.8

- 학생 선택 단계에서는 무거운 분석을 미루고 `선택한 학생 조회`를 누를 때 계산하도록 개선했습니다.
- 다운로드 파일 생성과 반복 DB 저장을 필요한 시점까지 미뤄 학생 분석 첫 표시 속도를 개선했습니다.
- 학년·반·번호를 함께 사용해 학생을 더 정확하게 찾고 선택 상태를 안정적으로 유지하도록 보강했습니다.
- 워드클라우드용 TF-IDF 단어 수와 이미지 해상도를 높이고 결과를 캐시하도록 개선했습니다.
- 나이스 원본 파일은 `.xlsx.data` 형식만 사용하도록 안내와 검증을 명확히 했습니다.

## v1.0.7

- 학생 분석 화면의 상태 카드 구성을 `전체/창체/교과/행발` 4칸으로 재정리했습니다.
- 학생 선택 패널에 `학년 필터`, `반 필터`를 추가해 원하는 반을 빠르게 조회할 수 있도록 개선했습니다.
- UI 파인튜닝 과정에서 발생한 `app.py` 문자열/인코딩 깨짐 구간을 복구해 실행 안정성을 높였습니다.

## v1.0.6

- 하단 명함 카드 영역의 좌우 정렬과 타이포 계층을 개선했습니다.
- 저작권/안내 문구의 가독성을 높이고 모바일 폭에서도 레이아웃이 안정적으로 보이도록 조정했습니다.

## v1.0.5

- Added dependency audit for bundled analyzers and Kiwi model runtime checks.
- Added explicit analyzer setup guidance for optional MeCab installation path.

## v1.0.3

- 하단 정보를 Streamlit 기본 구성요소로 변경하여 HTML 코드가 그대로 노출될 가능성 제거
- `VERSION`을 빌드 버전의 단일 기준으로 사용하고 소스·산출물 버전 일치 검증 추가
- 이전 빌드 산출물을 안전하게 정리한 뒤 새로 만드는 클린 빌드 절차 추가
- ZIP·설치본과 SHA-256 빌드 정보의 일치 여부를 자동 검증
- UTF-8 손상, 대체 문자와 알려진 한글 깨짐 문자열을 배포 전 자동 검사

## v1.0.2

- 업데이트 설치 시 실행 중인 구버전을 자동으로 종료하도록 개선
- 중단된 설치나 수동 삭제 뒤 남은 프로그램 파일을 새 설치가 자동 정리하도록 개선
- 제거 시 실행 중인 프로그램을 먼저 종료하여 삭제 충돌 방지
- 사용자 분석 데이터는 업데이트와 제거 과정에서 보존

## v1.0.1

- Windows 배포판에서 `pyparsing`이 요구하는 표준 라이브러리 `unittest`가 누락되어 앱 화면이 열리지 않던 문제 수정
- 기존 v1.0.0 설치 위에 업데이트할 수 있는 패치 설치본 제공

## v1.0.0

학생부 탐색기의 첫 정식 공개 배포판입니다.

- 학생부 창체·교과세특·행발 전처리와 학생별 통합
- 빈도·TF-IDF 특징어·워드클라우드 분석
- 추출 키워드의 학생부 근거 문장 확인
- 커리어넷 학과 말뭉치 연계도와 공통·보완 키워드 분석
- 학생별 최상위 유사 학과 자동 선택
- 학생부 기반 불용어 검토 후보와 표현 통일 사전
- Windows 설치본과 무설치본 제공
- 사용자 데이터와 설치 파일의 저장 위치 분리
